{
    "mongo_exec": {
        "options": [],
        "before": [],
        "args": "",
        "queries": {
            "desc" : {
                "query": "use %s; show collections",
                "format" : "|%s|"
            },
            "show records": {
                "query": "db.%s.find().limit(100)",
                "format" : "|%s|"
            }
        }
    }
}
